import 'package:flutter/material.dart';

class SheetsErrorIndicator extends StatelessWidget {
  const SheetsErrorIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Alguma coisa deu errado.'),
    );
  }
}
