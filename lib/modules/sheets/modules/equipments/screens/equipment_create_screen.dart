import 'package:flutter/material.dart';

class EquipmentCreateScreen extends StatelessWidget {
  const EquipmentCreateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
