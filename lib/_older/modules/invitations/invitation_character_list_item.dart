import 'package:flutter/material.dart';

class InvitationCharacterListItem extends StatelessWidget {
  const InvitationCharacterListItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}